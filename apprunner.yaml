version: 1.0

# AWS App Runner configuration file for hn-today-api

runtime: nodejs22

run:
  command: npm start

healthCheck:
  path: /
  interval: 10
  timeout: 5
  healthyThreshold: 1
  unhealthyThreshold: 5
